{
  "name": "@agent-platform/cli",
  "version": "0.1.0",
  "description": "Control plane CLI for the agent-platform harness-agnostic agent engineering platform (spec F.5 / DESIGN.md §6).",
  "license": "UNLICENSED",
  "private": true,
  "bin": {
    "agent": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node scripts/clean.js",
    "build": "npm run clean && tsc -p tsconfig.json && node scripts/copy-assets.js",
    "test": "node --import tsx --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "commander": "^12.1.0",
    "yaml": "^2.5.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.2",
    "tsx": "^4.19.1",
    "typescript": "^5.5.4"
  }
}
